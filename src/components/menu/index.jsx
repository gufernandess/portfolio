import { MenuContainer } from "./styles";
import Navbar from "../navbar";
import Social from "../social";

const Menu = () => {
    return (
        <MenuContainer>
            <Navbar />
            <Social />
        </MenuContainer>
    );
};

export default Menu;